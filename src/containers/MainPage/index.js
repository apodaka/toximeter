import * as React from "react";
import Calendar from "../../components/Calendar";
import T from "prop-types";

/** Pagina pricipal que muestra el calendario */
export const MainPage = () => {
  return (
    <div>
      <Calendar />
    </div>
  );
};

export default MainPage;
